//add two complex numbers using structure.
#include <stdio.h>
int main () {


struct complex {
    int real;
    int img;
};
struct complex A,B;
printf ("enter the real part:");
scanf("%d",&A.real);
printf ("enter the real part:");
scanf("%d",&B.img);
printf("%d + %di",A.real,B.img);

return 0;
}